
/**
 * Return a string of the first 5 characters in s.
 * If s has less than 5 characters, return s.
 * @param {String} s 
 */
exports.sliceString = function(s) {
  
}


/**
 * Return the subarray of arr that begins at index 2 and ends at index
 * 5, inclusive. If the array is does not contain index 2 or index 5, 
 * return an empty array.
 * @param {*[]} arr 
 */
exports.sliceArray = function(arr) {

}
